import React from "react";
import convert from "convert-units";

const ForecastValue = (overlay, units, setUnits, value) => {
  const convertedValue = value;
  return <>{convertedValue}</>;
};

export default ForecastValue;
